# No dot-directories except github/vscode
.*/
!.vscode/
!.github/

# Environment files (keep .env.example)
.env
!.env.example

# Build artifacts
# Note: bin/ contains pre-built binaries for distribution, so it's tracked in git
cert/
bridge/cert/*.pem

# Test coverage files
bridge/coverage.out

*.code-workspace
node_modules
nbproject

# npm package files
iobroker.*.tgz

Thumbs.db
.commitinfo

# i18n intermediate files
admin/i18n/flat.txt
admin/i18n/*/flat.txt